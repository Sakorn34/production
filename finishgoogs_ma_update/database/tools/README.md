# database/tools — ย้ายไป archive แล้ว

สคริปต์ CLI ทั้งหมดอยู่ที่:

`production/_archive/dev-tools/finishgoogs_ma_update/database/tools/`

ตัวอย่าง:

```bash
cd D:\AppServ\www\production
php _archive/dev-tools/finishgoogs_ma_update/database/tools/qc_localhost.php
```

โฟลเดอร์ `database/import/` และ `database/import_log/` ยังอยู่ที่เดิม (ใช้โดย `updates_import.php`)
