# ไฟล์ในโฟลเดอร์นี้ต้องรันจาก Plesk Scheduled Task (CLI) เท่านั้น — ห้ามเรียกผ่านเว็บ
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
