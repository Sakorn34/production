AddDefaultCharset UTF-8

# ห้ามเข้าถึงไฟล์ที่มีข้อมูลอ่อนไหว/ไม่ควรเข้าถึงตรงจากเว็บ
<FilesMatch "\.(json|db|log|ps1|md)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>
