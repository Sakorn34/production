# ป้องกันไฟล์ log/ไฟล์ระบบที่หลุดมาอยู่ที่ root ไม่ให้เข้าถึงตรงจากเว็บ
<FilesMatch "\.(log)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>
